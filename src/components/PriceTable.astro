---

const prices = [
  {
    title: "Free Plan",
    price: "0",
    description: "Get started with a self-hosted website and essentials.",
    features: [
      "Free website hosting",
      "Basic SEO setup",
      "Starter templates",
      "Community support",
      "Dashboard access"
    ]
  },
  {
    title: "Chill Plan",
    price: "500",
    description: "Perfect for small businesses ready to grow without the grind.",
    features: [
      "1 social post per week",
      "Verified email scraping (up to 500)",
      "Ad campaign setup",
      "Priority support",
      "Live dashboard metrics"
    ]
  },
  {
    title: "SuperChill Plan",
    price: "2000",
    description: "The complete marketing package — website, social, and email managed for you.",
    features: [
      "Website CRO Max (funnels, VSL, auto-replies)",
      "SEO Max (Google + Maps + Reddit)",
      "Weekly posts + creatives + ads",
      "Email scraping + outreach",
      "24/7 AI + human support"
    ]
  }
]


---

<section>
    <div class="container">
        <div class="max-w-lg m-auto text-center">
            <h1 class="text-xlg">Chill pricing</h1>
            <p class="text-sm m-auto max-w-md">Choose the best plan for your business</p>
       </div>
        <div class="price-wrap gap-3">
            {prices.map((item) => (
                    <div class="price-item radius-md">
                        <h3 class="text-md text-bold">{item.title}</h3>
                        <div class="item-cost flex align-center gap-1">
                            <h1 class="text-lg">${item.price}</h1>
                            <p class="text-sm text-normal"> /per month</p>
                        </div>
                        <p class="price-desc">{item.description}</p>
                        <ul class="price-feature">
                            {item.features.map((el) => (
                                <li class="price-feature-item flex align-center gap-1 mt-1">
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6">
                                      <path fill-rule="evenodd" d="M2.25 12c0-5.385 4.365-9.75 9.75-9.75s9.75 4.365 9.75 9.75-4.365 9.75-9.75 9.75S2.25 17.385 2.25 12Zm13.36-1.814a.75.75 0 1 0-1.22-.872l-3.236 4.53L9.53 12.22a.75.75 0 0 0-1.06 1.06l2.25 2.25a.75.75 0 0 0 1.14-.094l3.75-5.25Z" clip-rule="evenodd" />
                                    </svg>
                                    <span>{el}</span>
                                </li>    
                            ))}
                        </ul>
                        <a href="#" class="button w-full">Get Started for Free</a>
                    </div>
                ))
            }
       </div>
    </div>
</section>

<style>


section{
    padding-top: 150px;
}

.price-wrap{
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    padding-top: 100px;
}
.price-item{
    padding: var(--radius-lg);
    border: var(--border);
}

.price-feature{
    min-height: 230px;
}


svg{
    fill: var(--accent);
    width: 20px;
}
ul{
    list-style-type: none;
    padding: 0;
    margin: 50px 0 0 0;
}


@media (max-width: 700px) {
    .price-wrap{
        grid-template-columns: 100%;
    }
}

</style>
