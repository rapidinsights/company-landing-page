---

const prices = [
  {
    title: "Data Command Center",
    price: "Custom",
    description: "Your starting point. Escape spreadsheet chaos.",
    features: [
      "3 data sources connected",
      "Daily automated updates",
      "Unlimited custom dashboards",
      "Reports in seconds",
      "Perfect for: Escaping spreadsheet chaos"
    ]
  },
  {
    title: "Scale & Analyze",
    price: "Custom",
    description: "Expand your capabilities. For complex needs and long-term growth.",
    features: [
      "Everything in Command Center",
      "Unlimited data sources",
      "Ongoing analytics partnership",
      "Advanced reporting & trends",
      "Monthly optimization"
    ]
  },
  {
    title: "AI-Ready",
    price: "Custom",
    description: "Add intelligence. Move beyond dashboards.",
    features: [
      "Requires Command Center first",
      "Predictive analytics",
      "Automated anomaly detection",
      "Smart recommendations",
      "Custom ML features"
    ]
  }
]


---

<section>
    <div class="container">
        <div class="max-w-lg m-auto text-center">
            <h1 class="text-xlg">Built for Your Growth</h1>
            <p class="text-sm m-auto max-w-md">Start with automated dashboards. Scale with advanced analytics. Add AI when you're ready.</p>
       </div>
        <div class="price-wrap gap-3">
            {prices.map((item) => (
                    <div class="price-item radius-md">
                        <h3 class="text-md text-bold">{item.title}</h3>
                        <div class="item-cost flex align-center gap-1">
                            <h1 class="text-lg">{item.price}</h1>
                            <p class="text-sm text-normal"> pricing</p>
                        </div>
                        <p class="price-desc">{item.description}</p>
                        <ul class="price-feature">
                            {item.features.map((el) => (
                                <li class="price-feature-item flex align-center gap-1 mt-1">
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6">
                                      <path fill-rule="evenodd" d="M2.25 12c0-5.385 4.365-9.75 9.75-9.75s9.75 4.365 9.75 9.75-4.365 9.75-9.75 9.75S2.25 17.385 2.25 12Zm13.36-1.814a.75.75 0 1 0-1.22-.872l-3.236 4.53L9.53 12.22a.75.75 0 0 0-1.06 1.06l2.25 2.25a.75.75 0 0 0 1.14-.094l3.75-5.25Z" clip-rule="evenodd" />
                                    </svg>
                                    <span>{el}</span>
                                </li>    
                            ))}
                        </ul>
                        <a href="/#contact" class="button w-full">Book Free Call</a>
                    </div>
                ))
            }
       </div>
    </div>
</section>

<style>


section{
    padding-top: 150px;
}

.price-wrap{
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    padding-top: 100px;
}
.price-item{
    padding: var(--radius-lg);
    border: var(--border);
}

.price-feature{
    min-height: 230px;
}


svg{
    fill: var(--accent);
    width: 20px;
}
ul{
    list-style-type: none;
    padding: 0;
    margin: 50px 0 0 0;
}


@media (max-width: 700px) {
    .price-wrap{
        grid-template-columns: 100%;
    }
}

</style>
