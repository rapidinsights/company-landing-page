---
---

<section id="contact" class="contact-section">
    <div class="container">
        <div class="center text-container max-w-md m-auto">
            <h2 class="text-lg">Ready to escape spreadsheet chaos?</h2>
            <p class="text-sm">Book a free call and get your customized 30-day roadmap. See exactly how you'll start making confident, data-driven decisions.</p>
        </div>

        <div class="row">
            <div class="six columns contact-info">
                <div class="contact-item">
                    <div class="contact-icon">
                        <svg class="icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                        </svg>
                    </div>
                    <div class="contact-details">
                        <h4 class="text-bold">Email Us</h4>
                        <p>info@getrapidinsights.com</p>
                    </div>
                </div>

                <div class="contact-item">
                    <div class="contact-icon">
                        <svg class="icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                        </svg>
                    </div>
                    <div class="contact-details">
                        <h4 class="text-bold">Visit Us</h4>
                        <p>192 Spainda Ave., Toronto ON M5T 2C2, Canada</p>
                    </div>
                </div>

                <div class="social-section">
                    <h4 class="text-bold">Follow Us</h4>
                    <div class="social-links">
                        <a href="https://www.linkedin.com/company/rapid-insights-consulting-inc" aria-label="LinkedIn" target="_blank">
                            <svg class="social-icon" fill="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"></path>
                            </svg>
                        </a>
                        <a href="https://github.com/rapidinsights" aria-label="GitHub" target="_blank">
                            <svg class="social-icon" fill="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                <path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"></path>
                            </svg>
                        </a>
                    </div>
                </div>
            </div>

            <div class="six columns contact-form-wrapper">
                <div class="form-card">
                    <h3 class="text-md text-bold">Let's talk about your data</h3>

                    <form id="contactForm" name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="contact-form">
                        <input type="hidden" name="form-name" value="contact" />
                        <p class="honeypot-field">
                            <label>
                                Don't fill this out if you're human: <input name="bot-field" type="text" tabindex="-1" autocomplete="off" />
                            </label>
                        </p>
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input
                                type="text"
                                id="name"
                                name="name"
                                class="u-full-width"
                                placeholder="Your name"
                                required
                            />
                            <span class="error-message" id="nameError"></span>
                        </div>

                        <div class="form-group">
                            <label for="email">Email</label>
                            <input
                                type="email"
                                id="email"
                                name="email"
                                class="u-full-width"
                                placeholder="your.email@example.com"
                                required
                            />
                            <span class="error-message" id="emailError"></span>
                        </div>

                        <div class="form-group">
                            <label for="message">Message</label>
                            <textarea
                                id="message"
                                name="message"
                                rows="5"
                                class="u-full-width"
                                placeholder="Your message"
                                required
                            ></textarea>
                            <span class="error-message" id="messageError"></span>
                        </div>

                        <button type="submit" class="button button-primary u-full-width">
                            Book Free Call
                        </button>
                    </form>

                    <div class="success-message" id="successMessage">
                        <svg class="success-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <h4 class="text-md text-bold">We'll be in touch soon!</h4>
                        <p>Thank you for reaching out. We'll contact you within 24 hours to schedule your free call and create your 30-day roadmap.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    const form = document.getElementById('contactForm') as HTMLFormElement | null;
    const successMessage = document.getElementById('successMessage') as HTMLElement | null;
    const nameInput = document.getElementById('name') as HTMLInputElement | null;
    const emailInput = document.getElementById('email') as HTMLInputElement | null;
    const messageInput = document.getElementById('message') as HTMLTextAreaElement | null;

    const nameError = document.getElementById('nameError') as HTMLElement | null;
    const emailError = document.getElementById('emailError') as HTMLElement | null;
    const messageError = document.getElementById('messageError') as HTMLElement | null;

    function validateEmail(email: string): boolean {
        const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        return re.test(email);
    }

    function clearErrors(): void {
        if (nameError) nameError.textContent = '';
        if (emailError) emailError.textContent = '';
        if (messageError) messageError.textContent = '';

        if (nameInput) nameInput.classList.remove('error');
        if (emailInput) emailInput.classList.remove('error');
        if (messageInput) messageInput.classList.remove('error');
    }

    function showError(element: HTMLElement, errorElement: HTMLElement, message: string): void {
        errorElement.textContent = message;
        element.classList.add('error');
    }

    if (form && successMessage && nameInput && emailInput && messageInput && nameError && emailError && messageError) {
        form.addEventListener('submit', function(e) {
            e.preventDefault();
            clearErrors();

            let isValid = true;

            // Validate name
            if (!nameInput.value.trim()) {
                showError(nameInput, nameError, 'Please enter your name');
                isValid = false;
            }

            // Validate email
            if (!emailInput.value.trim()) {
                showError(emailInput, emailError, 'Please enter your email');
                isValid = false;
            } else if (!validateEmail(emailInput.value)) {
                showError(emailInput, emailError, 'Please enter a valid email');
                isValid = false;
            }

            // Validate message
            if (!messageInput.value.trim()) {
                showError(messageInput, messageError, 'Please enter your message');
                isValid = false;
            }

            if (isValid) {
                // Submit to Netlify
                const formData = new FormData(form);

                fetch('/', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                    body: new URLSearchParams(formData as any).toString()
                })
                .then(() => {
                    // Hide form and show success message
                    form.style.display = 'none';
                    successMessage.style.display = 'block';

                    // Reset form
                    form.reset();

                    // Optional: Hide success message after 5 seconds and show form again
                    setTimeout(() => {
                        if (successMessage && form) {
                            successMessage.style.display = 'none';
                            form.style.display = 'block';
                        }
                    }, 5000);
                })
                .catch((error) => {
                    console.error('Form submission error:', error);
                    alert('There was an error submitting the form. Please try again.');
                });
            }
        });
    }
</script>

<style lang="scss">
.contact-section {
    background-color: var(--offwhite);

    .text-container {
        margin-bottom: 50px;
    }

    .contact-info {
        .contact-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 30px;

            .contact-icon {
                flex-shrink: 0;
                margin-top: 5px;

                .icon {
                    width: 28px;
                    height: 28px;
                    color: var(--accent);
                }
            }

            .contact-details {
                margin-left: 20px;

                h4 {
                    font-size: 1.8rem;
                    margin-bottom: 8px;
                }

                p {
                    font-size: 1.6rem;
                    color: #666;
                    margin: 0;
                }
            }
        }

        .social-section {
            margin-top: 40px;

            h4 {
                font-size: 1.8rem;
                margin-bottom: 15px;
            }

            .social-links {
                display: flex;
                gap: 15px;

                a {
                    color: #666;
                    transition: color 0.3s ease;

                    &:hover {
                        color: var(--accent);
                    }

                    .social-icon {
                        width: 24px;
                        height: 24px;
                    }
                }
            }
        }
    }

    .contact-form-wrapper {
        .form-card {
            background: #fff;
            padding: 40px;
            border-radius: var(--radius-lg);
            border: var(--border);
            box-shadow: var(--shadow);

            h3 {
                margin-bottom: 25px;
            }
        }

        .contact-form {
            .honeypot-field {
                position: absolute;
                left: -9999px;
                opacity: 0;
                pointer-events: none;
            }

            .form-group {
                margin-bottom: 20px;

                label {
                    display: block;
                    font-size: 1.4rem;
                    font-weight: 500;
                    margin-bottom: 8px;
                    color: #333;
                }

                input, textarea {
                    border: var(--border);
                    border-radius: var(--radius);
                    padding: 12px 16px;
                    font-size: 1.5rem;
                    transition: border-color var(--cubic), box-shadow var(--cubic);

                    &:focus {
                        border-color: var(--accent);
                        box-shadow: 0 0 0 3px rgba(26, 26, 26, 0.1);
                        outline: none;
                    }

                    &.error {
                        border-color: #e53e3e;
                    }
                }

                .error-message {
                    display: block;
                    color: #e53e3e;
                    font-size: 1.3rem;
                    margin-top: 5px;
                    min-height: 18px;
                }
            }

            button[type="submit"] {
                margin-top: 10px;
            }
        }

        .success-message {
            display: none;
            text-align: center;
            padding: 30px;
            background: rgba(26, 26, 26, 0.05);
            border-radius: var(--radius-md);

            .success-icon {
                width: 60px;
                height: 60px;
                color: var(--accent);
                margin: 0 auto 20px;
            }

            h4 {
                margin-bottom: 10px;
            }

            p {
                font-size: 1.5rem;
                color: #666;
                margin: 0;
            }
        }
    }

    @media (max-width: 700px) {
        .row {
            flex-direction: column;
        }

        .contact-info {
            margin-bottom: 40px;

            .contact-item {
                margin-bottom: 25px;
            }

            .social-section {
                margin-top: 30px;
            }
        }

        .contact-form-wrapper .form-card {
            padding: 20px;
        }
    }
}
</style>
