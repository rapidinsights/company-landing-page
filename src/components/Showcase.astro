---

import featOne from '../images/shots/one.svg'
import featTwo from '../images/shots/two.svg'
import featThree from '../images/shots/three.svg'

const showcase = [
    {
        title: 'Stop Manual Reporting',
        desc: `Your data updates automatically while you sleep. No more weekend report marathons or chasing down numbers from five different spreadsheets.`,
        image: featOne,
   },
   {
        title: 'Built for How You Work',
        desc: `We don't hand you a template and walk away. Your dashboards match your workflow, show what matters to you, and grow as your business does.`,
        image: featTwo,
   },
   {
        title: 'From Chaos to Clarity',
        desc: `One dashboard replaces dozens of spreadsheets. See everything at a glance. Drill down when you need details. Make decisions fast.`,
        image: featThree,
   }
]
---




<section>
    <div class="container" id="features">
       <div class="max-w-md m-auto text-center">
           <h2 class="text-lg">Reclaim Your Weekends</h2>
           <p class="text-sm">Stop spending hours compiling reports. We build data infrastructure that runs itself—fresh numbers every morning, custom dashboards that answer your questions instantly.</p>
       </div>


        <div class="items grid-half gap-3 mt-2">
            {showcase.map((item) => (
                <div class="item">
                    <div class="item-text">
                        <h3 class="text-lg">{item.title}</h3>
                        <p class="text-sm">{item.desc}</p>
                        <a href="#contact" class="button button-primary">Book Free Call</a>
                    </div>
                    <img src={item.image.src} alt={item.title} class="w-full" />
                </div>
            ))}
        </div>
    </div>
</section>


<style>




.items{
    padding-top: 50px;
}
.item{
    background-color: var(--offwhite);
    overflow: hidden;
    border-radius: var(--radius-lg);
    padding: 30px;
}
.item img{
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-alt);
}
.item .text-lg{
    font-size: 2.8rem;
}


.item:nth-child(3) {
  grid-column: 1 / -1; 
  display: grid;
  grid-template-columns: 40% 60%;
  align-items: center;
}
.item:nth-child(3) .item-text {
  max-width: 90%;
}
.item:nth-child(3) img {
    box-shadow: none;
}


@media (max-width: 700px) {
    .items.grid-half{
        display: block ;
    }
    .item:nth-child(3) {
        display: block;
    }
}

</style>